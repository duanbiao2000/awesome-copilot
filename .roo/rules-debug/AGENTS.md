# Project Debug Rules (Non-Obvious Only)

- If the documentation is not generating correctly, check the `eng/doc-generator.js` file.
- If collections are not validating, check the `eng/validate-collections.js` file.
- Use `console.log` for debugging. There is no special logging setup.
